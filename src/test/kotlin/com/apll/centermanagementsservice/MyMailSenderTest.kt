/*
package com.apll.centermanagementsservice

import com.apll.centermanagementsservice.config.CenterManagementsServiceApplication
import com.apll.centermanagementsservice.mail.MailOutputStream
import com.apll.centermanagementsservice.mail.User
import org.junit.Test
import org.junit.runner.RunWith
import org.slf4j.LoggerFactory
import org.springframework.beans.factory.annotation.Autowired
import org.springframework.boot.test.context.SpringBootTest
import org.springframework.messaging.MessageHeaders
import org.springframework.messaging.support.MessageBuilder
import org.springframework.test.context.junit4.SpringRunner
import org.springframework.util.MimeTypeUtils

@RunWith(SpringRunner::class)
@SpringBootTest(classes = arrayOf(CenterManagementsServiceApplication::class))
class MyMailSenderTest {

    val log = LoggerFactory.getLogger(MyMailSenderTest::class.java)


    @Autowired
    lateinit var stream: MailOutputStream

    */
/*@Test
    fun sentMailTest(){
        //Publishing Event When we create CVReport Successfully.
        var event = User("pkazad23@gmail.com","Testing-Purpose","Hello Java")
        val messageChannel = stream.publish()
        var message=messageChannel.send(MessageBuilder.withPayload(event)
                .setHeader(MessageHeaders.CONTENT_TYPE, MimeTypeUtils.APPLICATION_JSON)
                .build())
        log.info("Mail-Event-Raised :: {}", event)
    }*//*

}*/
